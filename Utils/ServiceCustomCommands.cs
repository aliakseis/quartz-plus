namespace Utils
{
    /// <summary>
    /// Custom commands service values(between 128 and 256)
    /// </summary>
    public enum ServiceCustomCommand
    {
        None = 0,
        StartCheck = 130,
    }
}